<h1>My Tasks</h1>

<% @tasks.each do | task | %>
<input type="checkbox" <%= task.completed ? "checked" : "" %>><%= link_to task.title, task_path(task) %><br>
<% end %>

<%= link_to "Add a new task", tasks_new_path %>
